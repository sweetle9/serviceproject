<%= stylesheet_link_tag 'postindex', media: 'all', 'data-turbolinks-track': 'reload'%>
<meta name="viewport" content="width=device-width, initial-scale=1">
<p id="notice"><%= notice %></p>

<%= form_tag posts_path, :method => 'get' do %>
  <p>
    <%= text_field_tag :starts_with, params[:starts_with] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %>

	<%= react_component 'O_AllPosts', { posts: @posts } %>
	<%= react_component 'O_AllPostsMob', { posts: @posts } %>
	
</div>

	<!-- <div class="all_post">
		<% @posts.each do |post| %>
			<%= link_to post do %>
			<div class="post_container">
				<div class="hov">

				</div>
				<div class="pic">
					<%= image_tag(post.image.url, class: 'fit-image', alt: 'Image') if post.image? %>
				</div>
				<div class="info">
					<p class="postname"><%= post.author %></p>
					<p class="postname"><%= post.name %></p>
					<p class="postname"><%= post.title %></p>
					<div class="downinfo">
						<p>@<%= User.find(post.user_id).username %></p>
					</div>
				</div>
			</div>
			<% end %>
		<% end %>
	</div> -->
</div>
